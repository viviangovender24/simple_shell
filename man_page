.Interactive
simple shell can be invoked by typing ./hsh.

.Non-interactive
simple shell can also be invoked via a pipe (e.g. echo "/bin/ls" | ./hsh).

.echo
.[arguments] prints to standard output the argument(s) given and prints a newline

.ls 
lists files in the current directory according to the flags given by user

.pwd prints working directories

.cd 
changes directory

.exit
exits out of simple shell and returns to user's original shell

.setenv
creates non-existing or modifies existing environmental variable
